{% extends "base.html.twig" %}

{% block title %}{{ wo_render_breadcrumbs({ separator: '>', viewTemplate: "WhiteOctoberBreadcrumbsBundle::titlebreadcrumbs.html.twig" }) }}{% endblock title %}


{% block menu %}
    {% include '::partials/menu.html.twig' %}
{% endblock menu %}

{% block content %}
<div class="container-rh">
        <div class="row title-edit">
            <h5 class="col s12 box-title-rh">Incluir Calendário</h5>
            <div class="divider grey title-divider"><p>1</p></div>
        </div>
    <div class="row">
        {{ form_start(form) }}
        <div class="col s3 field-filtro select-filtro">
            {{ form_label(form.descricao, null, {'label_attr': {'class': 'control-label label-rh'}}) }}
            {{ form_widget(form.descricao, {'attr': {'class': 'white input-rh'}}) }}
        </div>
        <div class="col s3 field-filtro select-filtro">
            {{ form_label(form.codFeriado, null, {'label_attr': {'class': 'control-label label-rh'}}) }}
            {{ form_widget(form.codFeriado, {'attr': {'class': 'form-control white basic-multiple'}}) }}
        </div>
    </div>
    <div class="row">
        <div class="col s6">
            <a  class="btn-floating white-text blue darken-4" href="{{ path('calendario_calendario_cadastro_list') }}">
                <i class="material-icons">arrow_back</i>
            </a>
        </div>
        <div class="col s6 right-align">
            <button type="submit" class="waves-effect waves-light btn blue darken-4" value="Create"><i class="material-icons left">save</i>Salvar</button>
            {{ form_end(form) }}
        </div>
    </div>
</div>

{% endblock content %}
