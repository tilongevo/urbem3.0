{% extends "base_home_cards.html.twig" %}

{% block title %}{{ wo_render_breadcrumbs({ separator: '>', viewTemplate: "WhiteOctoberBreadcrumbsBundle::titlebreadcrumbs.html.twig" }) }}{% endblock title %}


{% block menu %}
    {% include '::partials/menu.html.twig' %}
{% endblock menu %}

{% block content %}
<div class="container">
    <div class="row">
        <div class="col s11">
            <h4 class="blue-text text-darken-4 title-filtro">Calendario\CalendarioDiaCompensado edit</h4>
        </div>
        <div class="col s1 right-align">
            {{ form_start(delete_form) }}
            <h4><button class="btn btn-delete blue-text text-darken-4 white z-depth-0 hide-on-med-and-down tooltipped" data-position="right" data-delay="50" data-tooltip="Excluir Dia Compensado" value="Delete"><i class="material-icons">delete</i></button></h4>
            {{ form_end(delete_form) }}
        </div>
    </div>
    <div class="row">
        {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <div class="row">
            <div class="col s6">
                <a  class="btn-floating white-text blue darken-4" href="{{ path('calendario_calendario_dia_compensado_index') }}"><i class="material-icons">arrow_back</i></a>
            </div>
            <div class="col s6 right-align">
                <button type="submit" class="waves-effect waves-light btn blue darken-4" value="Edit">Salvar</button>
                {{ form_end(edit_form) }}
            </div>
        </div>
    </div>
</div>
{% endblock content %}
