{% extends "base_home_cards.html.twig" %}
{% block content %}
    <div style="text-align: center">
        <div class="container text-center">
            <h4 class="blue-text text-darken-4">{{ parametro.titulo }}</h4>
            <div class="divider grey lighten-1  title-divider"><p>1</p></div>
            <p style="    font-size: 19px; color: #757575;">
                {{ parametro.mensagem }}<br><br>
            </p>
        </div>
        {% if parametro.voltar is not null %}
            <a class="white-text btn" href="{{ parametro.voltar }}">
                <i class="material-icons left">arrow_back</i>
                Voltar para Página inicial
            </a>
        {% endif %}
    </div>
    <i class="fa fa-ban" aria-hidden="true" style="
       position: absolute;
       left: -110px;
       color: #fff;
       bottom: -30px;
       font-size: 36em;">
    </i>
{% endblock content %}