{% extends 'CoreBundle:Sonata/CRUD:base_show_field.html.twig' %}
{% block field %}
    {% if field_description.options.data is defined and field_description.options.data is not empty %}
        {{ field_description.options.data | raw }}
    {% else %}
        -
    {% endif %}
{% endblock field %}
