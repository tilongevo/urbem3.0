<div class="row">
    <div class="col s12 servidor-table">
        <a class="btn-flat tooltipped"
           data-position="left"
           data-delay="50"
           data-tooltip="Permissões">
            <i class="fa fa-lock left" aria-hidden="true"></i>
            {{ 'label.patrimonial.almoxarifado.centrodecusto.permissoes'|trans }}
        </a>
    </div>
    <table class="col s12 highlight bordered">
        <thead class="thead-rh">
        <tr class="sonata-ba-list-field-header">
            <th class="th-rh" width="5%">#</th>
            <th class="th-rh">{{ 'label.patrimonial.almoxarifado.centrodecusto.cgm'|trans }}</th>
            <th class="th-rh">{{ 'label.patrimonial.almoxarifado.centrodecusto.numcgm'|trans }}</th>
        </tr>
        </thead>
        <tbody>
        {% for permissao in object.fkAlmoxarifadoCentroCustoPermissoes %}
            {% if permissao.responsavel != 1 %}
                <tr>
                    <td class="td-rh">{{ loop.index }}</td>
                    <td class="td-rh">{{ permissao.fkSwCgm.nomCgm }}</td>
                    <td class="td-rh">Sim</td>
                </tr>
            {% endif %}
        {% endfor %}
        </tbody>
    </table>
</div>
