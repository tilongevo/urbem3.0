{% extends 'SonataAdminBundle:CRUD:base_show_field.html.twig' %}

{% block field %}
    {% if value|length > 0 %}
        <table class="">
            <thead>
            <tr class="sonata-ba-list-field-header">
                <th class="sonata-ba-list-field-header-text th-reducao" style="text-align: center !important; background-color: #dbdbdb !important;">{{ 'label.codigo'|trans }}</th>
                <th class="sonata-ba-list-field-header-text th-reducao" style="text-align: center !important; background-color: #dbdbdb !important;">{{ 'label.descricao'|trans }}</th>
            </thead>
            <tbody>
            {% for atributo in value %}
                <tr class="tr-rh">
                    <td>{{ atributo.fkAdministracaoAtributoDinamico.codAtributo }}</td>
                    <td>{{ atributo.fkAdministracaoAtributoDinamico.nomAtributo }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    {% else %}
        -
    {% endif %}
{% endblock %}