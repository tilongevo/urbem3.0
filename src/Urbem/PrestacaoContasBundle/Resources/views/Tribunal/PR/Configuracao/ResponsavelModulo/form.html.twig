<form id="form" method="post">
    <div class="box-body no-padding">
        <table class="table show-table">
            <tbody>
                <tr class="sonata-ba-view-container">
                    <th>{{ form_label(form.fkTceprTipoModulo) }}</th>
                    <td>
                        {% if form.vars.value.dtBaixa is null %}
                            {{ form_widget(form.fkTceprTipoModulo) }}
                            {{ form_errors(form.fkTceprTipoModulo) }}

                        {% else %}
                            {{ form.vars.value.fkTceprTipoModulo }}

                        {% endif %}
                    </td>
                </tr>

                <tr class="sonata-ba-view-container">
                    <th>{{ form_label(form.fkTceprTipoResponsavelModulo) }}</th>
                    <td>
                        {% if form.vars.value.dtBaixa is null %}
                            {{ form_widget(form.fkTceprTipoResponsavelModulo) }}
                            {{ form_errors(form.fkTceprTipoResponsavelModulo) }}

                        {% else %}
                            {{ form.vars.value.fkTceprTipoResponsavelModulo }}

                        {% endif %}
                    </td>
                </tr>

                <tr class="sonata-ba-view-container">
                    <th>{{ form_label(form.fkSwCgm) }}</th>
                    <td>
                        {% if form.vars.value.dtBaixa is null %}
                            {{ form_widget(form.fkSwCgm) }}
                            {{ form_errors(form.fkSwCgm) }}

                        {% else %}
                            {{ form.vars.value.fkSwCgm }}

                        {% endif %}
                    </td>
                </tr>

                <tr class="sonata-ba-view-container">
                    <th>{{ form_label(form.dtInicioVinculo) }}</th>
                    <td>
                        {% if form.vars.value.dtBaixa is null %}
                            {{ form_widget(form.dtInicioVinculo) }}
                            {{ form_errors(form.dtInicioVinculo) }}

                        {% else %}
                            {{ form.vars.value.dtInicioVinculo | date('m/d/Y') }}

                        {% endif %}
                    </td>
                </tr>
            </tbody>
        </table>
    </div>

    <input type="hidden" name="key" value="{{ key }}" />

    <div class="sonata-ba-collapsed-fields custom-form">
        <div class="sonata-ba-form-actions well well-small form-actions row">
            <div class="col s12">
                <div class="col s12 right-align initial">
                    <button type="submit" class="white-text blue darken-4 btn btn-success save" name="save_update"><i class="material-icons left">input</i>Salvar</button>
                </div>
            </div>
        </div>
    </div>
</form>

<style>
    .custom-form .select2-container {height: 34px !important;}
</style>

<script>
    $('select.form-control').select2();
    $('#form').submit( function(e){
        e.preventDefault();
    });

</script>